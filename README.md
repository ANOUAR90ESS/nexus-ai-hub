<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# 🚀 Nexus AI Hub

AI Tool Directory powered by Gemini AI - Discover and explore next-generation AI tools with live demos, authentication, and admin dashboard.

## ✨ Features

- 🤖 **AI Tool Directory** - Browse and discover AI tools generated by Gemini
- 🎨 **Live Demos** - Interactive demos for Image Studio, Video, Audio, and Chat
- 🔐 **Authentication** - Secure user authentication with Supabase
- 👨‍💼 **Admin Dashboard** - Manage tools and news articles
- 💳 **Pricing & Payment** - Integrated pricing pages
- 📱 **Responsive Design** - Works on all devices

## 🛠️ Tech Stack

- **Frontend**: React 19, TypeScript, Vite
- **Backend**: Express.js, Node.js
- **Database**: Supabase
- **AI Integration**: Google Gemini API
- **Styling**: Tailwind CSS, Lucide Icons

## 📦 Prerequisites

- Node.js 18+ 
- npm or yarn
- Supabase account
- Google Gemini API key

## 🚀 Deploy to Vercel

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/ANOUAR90ESS/nexus-ai-hub)

### Environment Variables for Vercel:

Add these in your Vercel project settings:

```
VITE_GEMINI_API_KEY=your_gemini_api_key
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

## 💻 Run Locally

### Frontend Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/ANOUAR90ESS/nexus-ai-hub.git
   cd nexus-ai-hub
   ```

2. Install dependencies:
   ```bash
   npm install
   ```

3. Copy `.env.example` to `.env.local`:
   ```bash
   cp .env.example .env.local
   ```

4. Configure environment variables in `.env.local`:
   ```env
   VITE_GEMINI_API_KEY=your_gemini_api_key_here
   VITE_SUPABASE_URL=your_supabase_project_url
   VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
   ```

5. Run the development server:
   ```bash
   npm run dev
   ```

### Backend Server Setup

1. Navigate to server directory:
   ```bash
   cd server
   ```

2. Install dependencies:
   ```bash
   npm install
   ```

3. Copy `.env.example` to `.env`:
   ```bash
   cp .env.example .env
   ```

4. Configure environment variables in `server/.env`:
   ```env
   SUPABASE_URL=your_supabase_project_url
   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
   PORT=4000
   ```

5. Run the backend:
   ```bash
   npm run dev
   ```

## 🔑 Getting API Keys

### Gemini API Key
1. Visit [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Sign in with your Google account
3. Create a new API key
4. Copy and paste it into your `.env.local`

### Supabase Credentials
1. Visit [Supabase Dashboard](https://supabase.com/dashboard)
2. Create a new project or select existing one
3. Go to Project Settings → API
4. Copy:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon/public key** → `VITE_SUPABASE_ANON_KEY`
   - **service_role key** → `SUPABASE_SERVICE_ROLE_KEY` (server only)

## 📁 Project Structure

```
nexus-ai-hub/
├── components/          # React components
│   ├── demos/          # Live demo components
│   ├── AdminDashboard.tsx
│   ├── AuthModal.tsx
│   └── ...
├── services/           # API services
│   ├── geminiService.ts
│   ├── supabase.ts
│   └── authService.ts
├── server/            # Express backend
│   ├── server.ts
│   └── package.json
├── App.tsx            # Main application
├── types.ts           # TypeScript types
└── vite.config.ts     # Vite configuration
```

## 🌐 Build for Production

```bash
npm run build
npm run preview
```

## 📝 License

MIT

## 🤝 Contributing

Contributions, issues, and feature requests are welcome!

---

Made with ❤️ using React, Gemini AI, and Supabase
